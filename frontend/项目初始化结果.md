# 前端项目初始化结果

## 一、项目概述

项目名称：学科大模型AI能力中心管理后台
技术栈：React 19 + TypeScript 5.9 + Vite 7 + Tailwind CSS 4

## 二、目录结构

```
frontend/
├── src/
│   ├── api/
│   │   └── index.ts                    # API客户端封装
│   ├── assets/
│   │   └── react.svg
│   ├── components/
│   │   ├── Card/
│   │   │   ├── CapabilityRecommendationCard.tsx    # AI能力推荐卡片
│   │   │   ├── CaseFeaturedCard.tsx                # 精选案例卡片
│   │   │   ├── DatasetLatestCard.tsx               # 最新数据集卡片
│   │   │   ├── ModelRecommendationCard.tsx         # 大模型推荐卡片
│   │   │   └── index.tsx
│   │   ├── Header/
│   │   │   └── index.tsx
│   │   ├── PageContainer/
│   │   │   └── index.tsx
│   │   ├── __tests__/
│   │   │   ├── CapabilityCard.test.tsx
│   │   │   ├── CaseCard.test.tsx
│   │   │   ├── DatasetCard.test.tsx
│   │   │   ├── ModelCard.test.tsx
│   │   │   └── StatsCard.test.tsx
│   │   ├── common/
│   │   │   ├── EntityCard.tsx
│   │   │   └── StatCard.tsx
│   │   ├── dashboard/
│   │   │   ├── CapabilityCard.tsx
│   │   │   ├── CaseCard.tsx
│   │   │   ├── DatasetCard.tsx
│   │   │   ├── ModelCard.tsx
│   │   │   └── StatsCard.tsx
│   │   └── layout/
│   │       ├── Breadcrumb.tsx
│   │       ├── Header.tsx
│   │       └── Sidebar.tsx
│   ├── pages/
│   │   ├── Home/
│   │   │   └── index.tsx                 # 首页组件
│   │   ├── __tests__/
│   │   │   └── Dashboard.test.tsx
│   │   └── dashboard/
│   │       ├── components/
│   │       │   ├── CapabilityRecommendations.tsx
│   │       │   ├── FeaturedCases.tsx
│   │       │   ├── ModelRecommendations.tsx
│   │       │   ├── NewDatasets.tsx
│   │       │   ├── OverviewStats.tsx
│   │       │   ├── QuickAccess.tsx
│   │       │   └── RecentActivities.tsx
│   │       └── index.tsx                 # 仪表盘组件
│   ├── services/
│   │   ├── __tests__/
│   │   │   └── dashboard.test.ts
│   │   ├── api.ts
│   │   └── dashboard.ts
│   ├── test/
│   │   ├── __mocks__/
│   │   │   └── mockData.ts
│   │   ├── helpers.tsx
│   │   └── setup.ts
│   ├── types/
│   │   ├── dashboard.ts                   # 仪表盘类型定义
│   │   └── index.ts                       # 基础类型定义
│   ├── App.tsx
│   ├── App.css
│   ├── main.tsx
│   └── index.css
├── public/
├── index.html
├── package.json
├── package-lock.json
├── tsconfig.json
├── tsconfig.app.json
├── tsconfig.node.json
├── vite.config.ts
├── vitest.config.ts
├── tailwind.config.js
├── eslint.config.js
├── .gitignore
└── README.md
```

## 三、核心代码

### 1. package.json - 项目依赖配置

```json
{
  "name": "frontend",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.11.2",
    "axios": "^1.13.6",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-redux": "^9.2.0",
    "react-router-dom": "^7.13.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.2.0",
    "@testing-library/user-event": "^14.6.1",
    "autoprefixer": "^10.4.27",
    "jsdom": "^26.0.0",
    "postcss": "^8.5.8",
    "tailwindcss": "^4.2.1",
    "vite": "^7.3.1",
    "vitest": "^3.0.7"
  }
}
```

### 2. main.tsx - 入口文件

```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

### 3. App.tsx - 根组件

```typescript
import Dashboard from './pages/dashboard'
import './App.css'

function App() {
  return <Dashboard />
}

export default App
```

### 4. index.css - Tailwind CSS 配置

```css
@import "tailwindcss";

@layer base {
  body {
    @apply bg-gray-50 text-gray-900;
    margin: 0;
    min-width: 320px;
    min-height: 100vh;
  }

  * {
    box-sizing: border-box;
  }
}

@layer utilities {
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
}
```

### 5. tailwind.config.js - Tailwind 配置

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#eff6ff',
          100: '#dbeafe',
          200: '#bfdbfe',
          300: '#93c5fd',
          400: '#60a5fa',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
          800: '#1e40af',
          900: '#1e3a8a',
        },
      },
    },
  },
  plugins: [],
}
```

### 6. API 客户端 (src/api/index.ts)

```typescript
import axios, { AxiosInstance } from 'axios';

class ApiClient {
  private instance: AxiosInstance;

  constructor() {
    this.instance = axios.create({
      baseURL: '/api',
      timeout: 10000,
      headers: {
        'Content-Type': 'application/json'
      }
    });

    // 请求拦截器
    this.instance.interceptors.request.use(
      (config) => {
        const token = localStorage.getItem('token');
        if (token) {
          config.headers.Authorization = `Bearer ${token}`;
        }
        return config;
      },
      (error) => Promise.reject(error)
    );

    // 响应拦截器
    this.instance.interceptors.response.use(
      (response) => response,
      (error) => {
        console.error('API请求错误:', error);
        return Promise.reject(error);
      }
    );
  }

  // 获取仪表盘统计数据
  async getDashboardStats() {
    const response = await this.instance.get('/dashboard/stats');
    return response.data;
  }

  // 获取大模型推荐数据
  async getModelRecommendations() {
    const response = await this.instance.get('/dashboard/models/recommendations');
    return response.data;
  }

  // 获取最新数据集数据
  async getLatestDatasets() {
    const response = await this.instance.get('/dashboard/datasets/latest');
    return response.data;
  }

  // 获取精选案例数据
  async getFeaturedCases() {
    const response = await this.instance.get('/dashboard/cases/featured');
    return response.data;
  }

  // 获取AI能力推荐数据
  async getCapabilityRecommendations() {
    const response = await this.instance.get('/dashboard/capabilities/recommendations');
    return response.data;
  }
}

export const apiClient = new ApiClient();
```

### 7. 首页组件 (src/pages/Home/index.tsx)

首页包含：
- 欢迎横幅区域
- AI能力推荐卡片
- 大模型推荐卡片
- 精选案例卡片
- 最新数据集卡片
- 快速操作卡片
- 页脚

### 8. 仪表盘组件 (src/pages/dashboard/index.tsx)

仪表盘包含：
- Header 顶部导航
- Sidebar 侧边栏
- OverviewStats 统计概览
- QuickAccess 快速访问
- ModelRecommendations 大模型推荐
- NewDatasets 最新数据集
- FeaturedCases 精选案例
- CapabilityRecommendations AI能力推荐
- RecentActivities 最近活动

## 四、类型定义

### 核心数据类型 (src/types/dashboard.ts)

| 类型 | 说明 |
|------|------|
| DashboardStatsResponse | 仪表盘统计响应 |
| ModelItem | 大模型项 |
| DatasetItem | 数据集项 |
| CaseItem | 案例项 |
| CapabilityItem | AI能力项 |
| ActivityItem | 活动项 |

## 五、组件卡片

### 已实现的卡片组件

1. **ModelRecommendationCard** - 大模型推荐卡片
   - 显示模型名称、类型、版本、状态
   - 显示准确率、推理速度等指标
   - 内置Mock数据

2. **DatasetLatestCard** - 最新数据集卡片
   - 显示数据集名称、格式、大小、样本数量
   - 显示质量评分、难度级别
   - 内置Mock数据

3. **CaseFeaturedCard** - 精选案例卡片
   - 显示案例标题、场景、分类
   - 显示相关模型、数据集
   - 内置Mock数据

4. **CapabilityRecommendationCard** - AI能力推荐卡片
   - 显示能力名称、分类、描述
   - 显示响应时间、可用性、成功率
   - 内置Mock数据

## 六、测试配置

### 已配置的测试框架

- **Vitest** - 单元测试框架
- **React Testing Library** - React组件测试
- **Jest DOM** - DOM断言库

### 已创建的测试文件

- src/components/__tests__/CapabilityCard.test.tsx
- src/components/__tests__/CaseCard.test.tsx
- src/components/__tests__/DatasetCard.test.tsx
- src/components/__tests__/ModelCard.test.tsx
- src/components/__tests__/StatsCard.test.tsx
- src/pages/__tests__/Dashboard.test.tsx
- src/services/__tests__/dashboard.test.ts

## 七、启动说明

### 1. 安装依赖

```bash
cd frontend
npm install
```

### 2. 开发模式启动

```bash
npm run dev
```

访问地址：http://localhost:5173

### 3. 构建生产版本

```bash
npm run build
```

### 4. 预览生产构建

```bash
npm run preview
```

### 5. 运行测试

```bash
# 运行所有测试
npm run test

# 运行测试并显示UI界面
npm run test:ui

# 生成测试覆盖率报告
npm run test:coverage
```

### 6. 代码检查

```bash
npm run lint
```

## 八、Vite 代理配置 (vite.config.ts)

如需与后端API对接，可在 vite.config.ts 中配置代理：

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true,
      },
    },
  },
})
```

## 九、特性总结

✅ React 19 + TypeScript 5.9 + Vite 7
✅ Tailwind CSS 4 样式框架
✅ 完整的目录结构
✅ API 客户端封装 (axios)
✅ 类型定义文件
✅ 首页和仪表盘组件
✅ 四个模块卡片组件（大模型、数据集、案例、AI能力）
✅ 布局组件（Header、Sidebar、Breadcrumb）
✅ 测试框架配置 (Vitest + Testing Library)
✅ ESLint 代码检查
✅ 内置 Mock 数据
